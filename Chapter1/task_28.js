function splitString(element, separator){
        var chon = element.split(separator);
        for( var i = 0; i <= chon.length; i++ ){  
        } 
         element.chon(0).toUpperCase() + element.slice(1);
    console.log(chon.join());
}
    var space = ' ';
    splitString('the quick brown fox', space);

